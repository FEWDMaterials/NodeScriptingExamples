var fs = require( 'fs' );


function copyFiles( readPath, writePath ) {
	// using the fs module
	// read the readPath file and console.log it to
	// your terminal
}

module.exports = {
	copy: copyFiles
}